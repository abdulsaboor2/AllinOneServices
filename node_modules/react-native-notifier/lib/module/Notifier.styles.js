var _StatusBar$currentHei;

import { StyleSheet, Platform, StatusBar } from 'react-native';
export default StyleSheet.create({
  container: {
    zIndex: 2,
    width: '100%',
    // @ts-ignore
    position: Platform.select({
      web: 'fixed',
      default: 'absolute'
    }),
    top: 0,
    left: 0
  },
  translucentStatusBarPadding: {
    paddingTop: (_StatusBar$currentHei = StatusBar.currentHeight) !== null && _StatusBar$currentHei !== void 0 ? _StatusBar$currentHei : 0
  }
});
//# sourceMappingURL=Notifier.styles.js.map